# Practice It #5: Reconcile Inventory

Implement `reconcile_inventory()` to apply received stock, sales, and spoilage to a starting inventory.

Return a dictionary containing remaining inventory, oversold items, and low-stock items at or below the provided threshold.

## Example Run
```console
python "src/05 Reconcile Inventory/solve.py"
```
