# Practice It #16: Aggregate Product Quantities

Read `data/OrderItems.csv` and aggregate total quantities by product ID.

Use a `defaultdict(int)` to accumulate each product's total quantity.

## Example Run
```console
python "src/16 Aggregate Product Quantities/solve.py"
```
