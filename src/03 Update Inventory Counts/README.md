# Practice It #3: Update Inventory Counts

Use a `Counter` to model product inventory, subtract sold items, and update the count when more items are made.

The final output should show the updated inventory after sales and restocking.

## Example Run
```console
python "src/03 Update Inventory Counts/solve.py"
```
