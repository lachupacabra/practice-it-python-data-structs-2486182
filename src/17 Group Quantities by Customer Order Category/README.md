# Practice It #17: Group Quantities by Customer Order Category

Implement nested grouping for order line quantities.

Use nested `defaultdict` objects to build a structure of `customer_id -> order_id -> category -> total quantity`, then return it as regular dictionaries.

## Example Run
```console
python "src/17 Group Quantities by Customer Order Category/solve.py"
```
