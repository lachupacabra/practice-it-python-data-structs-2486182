# Practice It #7: Check Palindrome

Implement `check_palindrome()` so it returns whether a word reads the same forward and backward.

Use a `deque` to compare characters from both ends of the word.

## Example Run
```console
python "src/07 Check Palindrome/solve.py"
```
