# Practice It #2: Get Common Products

Implement `get_common_products()` so it accepts a list of purchased product IDs and returns the three most common products.

Use `Counter.most_common()` to produce a list of `(product_id, count)` tuples.

## Example Run
```console
python "src/02 Get Common Products/solve.py"
```
