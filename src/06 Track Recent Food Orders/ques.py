from collections import deque


def main():
    foods = deque(maxlen=5)
    orders = ["DES003", "STA002", "ENT004", "ENT001"]
    # TODO:
    # Append a single item, extend with multiple items, and add one item to the left.
    print(foods)
    return


if __name__ == "__main__":
    main()
