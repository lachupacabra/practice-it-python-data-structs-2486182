# Practice It #6: Track Recent Food Orders

Use a fixed-length `deque` to track the most recent food orders.

Practice appending a single item, extending with multiple items, and adding an item to the left side of the queue.

## Example Run
```console
python "src/06 Track Recent Food Orders/solve.py"
```
