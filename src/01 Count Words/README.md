# Practice It #1: Count Words

Implement `count_words()` so it accepts a paragraph of text and returns a `Counter` containing the frequency of each word.

The function should lowercase the text, remove punctuation, split the paragraph into words, and count each word.

## Example Run
```console
python "src/01 Count Words/solve.py"
```
