# Practice It #12: Read Customer Records

Read `data/Customer.csv` and convert each row into a workable customer record.

Use the CSV header to create a `namedtuple`, then print or return the loaded customer records.

## Example Run
```console
python "src/12 Read Customer Records/solve.py"
```
