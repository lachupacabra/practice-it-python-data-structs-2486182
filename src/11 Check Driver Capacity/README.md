# Practice It #11: Check Driver Capacity

Create driver records with `namedtuple` and check whether a driver can take an order.

The solution should compare the driver's car capacity against the number of packages in the order.

## Example Run
```console
python "src/11 Check Driver Capacity/solve.py"
```
